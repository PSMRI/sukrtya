
remote: Create a pull request for 'dev_ops' on GitHub by visiting:
remote:      https://github.com/PSMRI/sukrtya/pull/new/dev_ops
configure you aws credentials
after that
change you directory to dev_ops
run command 
===> terraform init
===> terraform plan
===> terraform aplly
wait for some time , this csript create EKS cluster for you.
============================================================================================
after terrform cluster creation. run below command.This will allow to access kube cluster from local
=========================================================================
aws eks --region $(terraform output -raw region) update-kubeconfig \
    --name $(terraform output -raw cluster_name)
========================================================================
First, get information about the cluster.
==> kubectl cluster-info
Now verify that all three worker nodes are part of the cluster.
==>kubectl get nodes

Apply complete! Resources: 61 added, 5 changed, 0 destroyed.

Outputs:

cluster_endpoint = "https://375157334343AC3C65C8AFF57F172E31.gr7.ap-south-1.eks.amazonaws.com"     
cluster_name = "SUkrtya-eks-manish_piramal_1"
cluster_security_group_id = "sg-049a575f2c1bbe0ee"
region = "ap-south-1"
